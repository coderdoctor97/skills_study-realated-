# Claude Code Integration

To invoke the Univer PDF Converter within Claude Code:

- Use `/convert-to-pdf <file>` to trigger automatic publication-grade PDF generation.
- Use `/custom-pdf` to customize fonts, palettes, and page layouts.
- Use `/add [--cover] [--index] [--images]` to enrich the document before building.
